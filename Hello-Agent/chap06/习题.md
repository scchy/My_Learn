# 第六章习题

## 1. 智能体框架对比分析

本章介绍了四个各具特色的智能体框架：AutoGen、AgentScope、CAMEL 和 LangGraph。请分析以下问题：

### 1.1 框架对比

在 6.1.2 节的表 6.1 中，对比了这四个框架的多个维度。请选择其中两个你最熟悉的框架，从以下三个维度进行深入对比：

- **协作模式**
- **控制方式**
- **适用场景**  

| 维度       | AutoGen                                                                                                           | LangGraph                                                                                                    |
| :------- | :---------------------------------------------------------------------------------------------------------------- | :----------------------------------------------------------------------------------------------------------- |
| **协作模式** | **对话驱动（Conversation-Driven）**<br>将任务执行抽象为多角色参与的"群聊"。智能体通过自然语言对话进行协作，如产品经理→工程师→审查员的软件开发流程。协作行为从角色定义和对话规则中**自然涌现**。 | **状态机驱动（State Machine-Driven）**<br>将工作流显式建模为有向图，节点代表计算步骤（LLM调用/工具执行），边定义跳转逻辑。协作通过**预定义的图结构**严格执行，支持循环实现迭代反思。 |
| **控制方式** | **隐式控制**：通过系统消息（System Message）定义角色职责，通过 `RoundRobinGroupChat` 等机制控制发言顺序。控制逻辑分散在对话上下文中，灵活但难以精确预测执行路径。             | **显式控制**：开发者精确定义状态（State）、节点（Node）、边（Edge）及条件跳转。每个步骤都是确定的 Python 函数，支持"人机协作"节点插入，可控性强但牺牲了动态性。                |
| **适用场景** | 适合**开放式、创造性任务**：<br>• 多角色头脑风暴<br>• 需求不明确需反复澄清的项目<br>• 需要"人类在环"（Human-in-the-loop）的协作场景                            | 适合**流程化、高可靠性任务**：<br>• 需要精确步骤控制的业务流程<br>• 需审计、可复现的生产级应用<br>• 包含"反思-修正"循环的复杂工作流                               |


### 1.2 设计哲学理解

本章提到了"涌现式协作"与"显式控制"之间的权衡，请解释这两种设计哲学的含义。

涌现式协作：  通过定义智能体的角色、目标和简单交互规则，让复杂的协作行为从系统中自发"生长"出来，而非被显式编码。  
  - 代表框架：AutoGen（群聊模式）、CAMEL（角色扮演）  

显式控制：开发者精确定义智能体执行的每一个步骤、状态转移条件和循环逻辑，将工作流固化为可验证的结构。   
 - 代表框架：LangGraph（状态机）、传统工作流引擎

| 维度          | 涌现式协作       | 显式控制         |
| :---------- | :---------- | :----------- |
| **开发速度**    | 快（定义角色即可启动） | 慢（需完整建模流程）   |
| **运行时可预测性** | 低           | 高            |
| **容错能力**    | 强（可协商恢复）    | 弱（需预设错误处理分支） |
| **适用任务类型**  | 探索性、创造性     | 流程化、事务性      |


选择原则：
- 任务边界模糊、需要多方协商 → 涌现式协作（AutoGen/CAMEL）
- 任务步骤明确、需要严格合规 → 显式控制（LangGraph）

---

## 2. AutoGen 案例扩展

在 6.2 节的 AutoGen 案例中，我们构建了一个"软件开发团队"。请基于此案例进行扩展思考：

> 💡 **提示**：这是一道动手实践题，建议实际操作

### 2.1 动态回退机制

当前的团队使用 `RoundRobinGroupChat`（轮询群聊）模式，智能体按固定顺序发言。如果需求变更，工程师的代码需要返回给产品经理重新审核，应该如何修改协作流程？请设计一个支持**动态回退**的机制。

详见CODE，增加状态机显示管理 [play_autogen.StateMachineOrchestrator](./play_autogen.py)

### 2.2 添加新角色

在案例中，我们通过 System Message 为每个智能体定义了角色和职责。请尝试为这个团队添加一个新角色**测试工程师**（Quality Assurance），并设计其系统消息，使其能够在代码审查后执行自动化测试。

同样基于状态机显示管理调整了code [play_autogen.StateMachineOrchestrator](./play_autogen.py)

### 2.3 对话质量监控

AutoGen 的对话式协作存在可能的不稳定性，可能导致对话偏离主题或陷入循环。请思考：如何设计一套**对话质量监控**机制，在检测到异常时及时干预？

---

## 3. AgentScope 案例深入分析

在 6.3 节的 AgentScope 案例中，我们实现了一个"三国狼人杀"游戏。请深入分析：

### 3.1 消息驱动架构

案例中使用了 `MsgHub`（消息中心）来管理智能体间的通信。请回答：

- 消息驱动架构相比传统函数调用的优势是什么？
- 在什么场景下这种架构特别有价值？

### 3.2 结构化输出设计

游戏中使用了结构化输出（如 `DiscussionModelCN`、`WitchActionModelCN`）来约束智能体行为。请设计一个新的游戏角色**猎人**，并定义其对应的结构化输出模型，包括：

- 字段定义
- 验证规则

### 3.3 分布式部署挑战

AgentScope 支持分布式部署，这意味着不同的智能体可以运行在不同的服务器上。请思考：

- 在"三国狼人杀"这样的实时游戏场景中，分布式部署会带来哪些技术挑战？
- 如何保证消息的顺序性和一致性？

---

## 4. CAMEL 案例分析

在 6.4 节的 CAMEL 案例中，我们让心理学家和作家协作创作电子书。

### 4.1 冲突解决机制

在案例中，协作会在检测到 `<CAMEL_TASK_DONE>` 标志时强制终止。但如果两个智能体意见分歧（一位认为可以终止，一位认为不应该终止），无法达成一致怎么办？请设计一个**冲突解决**的兼容机制。

### 4.2 多智能体协作

CAMEL 最初设计用于双智能体协作，但现在已经扩展支持多智能体。请查阅 CAMEL 的最新文档，了解其多智能体协作模块 `workforce`，并结合架构图说明其与 AutoGen 的群聊模式有何不同。

---

## 5. LangGraph 案例分析

在 6.5 节的 LangGraph 案例中，我们构建了一个"三步问答助手"。请分析：

### 5.1 流程图绘制

LangGraph 将智能体流程建模为状态机和有向图。请画出案例中"理解 → 搜索 → 回答"流程的图结构，标注：

- 节点
- 边
- 状态转换条件

### 5.2 添加反思节点

当前的助手是一个线性流程。请扩展这个案例，添加一个**反思**节点：如果生成的答案质量低（例如过于简短或缺乏细节），系统应该重新搜索或重新生成答案。请设计这个循环机制的**条件边逻辑**。

### 5.3 循环特性应用

LangGraph 的优势在于对循环的原生支持。请设计一个更复杂的应用场景，充分利用这一特性，例如：

- "代码生成 → 测试 → 修复"循环
- "论文写作 → 审阅 → 修改"循环

要求：

- 画出完整的图结构
- 说明关键节点的功能

---

## 6. 框架选型决策

框架选型是智能体产品开发过程中的关键决策之一。假设你是一家 AI 公司的技术架构师，公司计划开发以下三个智能体产品应用，请为每个应用选择最合适的框架（AutoGen、AgentScope、CAMEL、LangGraph 或不借助框架从零开发），并详细说明理由。

### 应用 A：智能客服系统

**需求**：

- 处理大量并发用户请求（每秒 1000+）
- 响应时间低于 2 秒
- 7 × 24 小时稳定运行
- 支持水平扩展

**选型**：__________  
**理由**：

### 应用 B：科研论文辅助写作平台

**需求**：

- "研究员智能体"和"写作智能体"深度协作
- 共同完成文献综述、实验设计、数据分析和论文撰写
- 支持多轮深度讨论
- 自主推进任务

**选型**：__________  
**理由**：

### 应用 C：金融风控审批系统

**需求**：

- 严格流程：资料审核 → 风险评估 → 额度计算 → 合规检查 → 人工复核 → 最终决策
- 每个环节都有明确的判断标准和分支逻辑
- 流程可追溯、可审计

**选型**：__________  
**理由**：
