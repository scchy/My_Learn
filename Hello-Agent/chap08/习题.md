# 第八章习题：记忆系统与 RAG 技术

> **学习目标**：通过本章习题，深入理解四种记忆类型的工作机制，掌握 RAG 系统的核心优化策略，并能够针对实际场景设计完整的记忆+RAG解决方案。
>
> **建议用时**：理论题 30~45 分钟，动手实践题 60~90 分钟。
> ---

---

## 1. 记忆类型对比与应用设计 ⭐⭐ 理论/设计

本章介绍了四种记忆类型：**工作记忆**、**情景记忆**、**语义记忆**和**感知记忆**。请完成以下分析：

### 1.1 评分机制对比

在 8.2.5 节中，每种记忆类型都有独特的评分公式。

- 请对比**情景记忆**和**语义记忆**的评分机制。
- 解释为什么情景记忆更强调「时间近因性」（权重 0.2），而语义记忆更强调「图检索」（权重 0.3）？
- *提示：从两种记忆类型在人类认知中的功能差异出发思考。*

### 1.2 应用场景设计

如果要设计一个「**个人健康管理助手**」（需要记录用户的饮食、运动、睡眠数据，并提供健康建议），你会如何组合使用这四种记忆类型？

| 记忆类型 | 应用场景 | 存储内容示例 | 作用 |
|---------|---------|-------------|------|
| 工作记忆 | （请填写） | 当前对话中的临时指标 | 维持短期上下文 |
| 情景记忆 | （请填写） | | |
| 语义记忆 | （请填写） | | |
| 感知记忆 | （请填写） | | |

### 1.3 记忆整合机制

工作记忆采用 TTL（Time To Live）机制自动清理过期数据。

- 在什么情况下，重要的工作记忆应该被「整合」（consolidate）为长期记忆？
- 请设计一个**自动整合的触发条件**（如：访问频率阈值、情感标记、显式用户指令等），并说明你的设计理由。

---

## 2. RAG 系统优化 ⭐⭐⭐ 动手实践

在 8.3 节的 RAG 系统中，我们使用 MarkItDown 将各种格式文档统一转换为 Markdown。

> **提示**：以下子题为动手实践题，建议准备 3~5 份不同格式的测试文档（PDF、Word、网页等）实际操作。

### 2.1 智能分块策略优化

当前的智能分块策略基于 Markdown 的标题层次（`#`、`##`、`###`）进行分割。

- **问题**：如果处理的是**没有明确标题结构**的文档（如小说、法律条文、聊天记录），标题分块会失效。
- **任务**：请设计并实现一个基于「**语义边界**」的分块算法。
  - 思路参考：利用句子嵌入的相似度变化检测主题转换点；或基于 LLM 判断段落边界。
  - 输出要求：提供核心代码片段（Python），并说明你的算法时间复杂度。

### 2.2 高级检索策略对比

在 8.3.5 节中介绍了 MQE（多查询扩展）和 HyDE（假设文档嵌入）两种高级检索策略。

- 请选择一个实际场景（如：技术文档问答、医疗知识检索、法律法规查询）。
- 对比 **基础检索**、**MQE** 和 **HyDE** 三种方法的效果差异。
- 分析各自的适用场景，并填写下表：

| 检索策略 | 优点 | 缺点 | 最适用场景 |
|---------|------|------|-----------|
| 基础检索 | | | |
| MQE | | | |
| HyDE | | | |

### 2.3 嵌入模型选型

RAG 系统的检索质量很大程度上取决于嵌入模型的选择。

- 请对比本章提到的三种嵌入方案：**百炼 API**、**本地 Transformer**、**TF-IDF**。
- 从以下维度进行评估：准确性、速度、成本、离线部署能力、多语言支持。
- 给出你的**选型建议矩阵**（不同场景推荐不同方案）。

---

## 3. 记忆系统的遗忘与归档 ⭐⭐⭐ 动手实践

记忆系统的「遗忘」机制是模拟人类认知的重要设计。基于 8.2.3 节的 `MemoryTool`，请完成以下扩展实践：

> **提示**：建议 fork 本章代码，在 `memory_tool.py` 基础上进行扩展。

### 3.1 智能遗忘策略

当前提供了三种遗忘策略：基于重要性、基于时间、基于容量。

- **任务**：设计并实现一个「**智能遗忘**」策略。
  - 要求：综合考虑**重要性**、**访问频率**、**时间衰减**、**情感标记**等多个因素。
  - 使用**加权评分**来决定哪些记忆应该被遗忘。
  - 输出要求：提供评分公式和核心代码实现。

```python
# 参考框架
def intelligent_forgetting_score(memory) -> float:
    """
    返回记忆的遗忘评分，分数越高越应该被删除。
    请补充完整公式。
    """
    importance = memory.importance  # 0~1
    access_freq = memory.access_count / total_access
    time_decay = exp(-lambda * (now - memory.last_access))
    # TODO: 你的公式
    pass
```

### 3.2 记忆归档机制

在长期运行的智能体系统中，记忆数据库可能会积累大量数据。

- **任务**：设计一个「**记忆归档**」机制。
  - 将长期不用但可能有价值的记忆转移到**冷存储**（如文件系统、低成本对象存储），需要时再恢复。
  - 说明该机制如何与现有的四种记忆类型集成？
  - 请画出简单的流程图或状态转换图（可用文字描述）。

### 3.3 敏感数据彻底清除

思考：如果智能体需要「忘记」某些敏感信息（如用户隐私数据），仅仅从数据库删除是否足够？

- 在使用**向量数据库**（如 Qdrant）和**图数据库**（如 Neo4j）的情况下，如何确保数据被彻底清除？
- 讨论：是否存在「残余嵌入」或「关系残留」的风险？

---

## 4. 智能学习助手案例分析 ⭐⭐⭐ 理论/设计

在 8.4 节的「智能学习助手」案例中，我们结合了 `MemoryTool` 和 `RAGTool`。

### 4.1 检索方式智能路由

案例中的 `ask_question()` 方法同时使用了 RAG 检索和记忆检索。

- **分析**：在什么情况下应该**优先使用 RAG**？在什么情况下应该**优先使用 Memory**？
- **任务**：设计一个「**智能路由**」机制，自动选择最合适的检索方式。
  - 输入：用户查询 + 当前上下文
  - 输出：路由决策（RAG / Memory / 混合）
  - 请描述你的决策逻辑，可以用伪代码或规则引擎形式表达。

### 4.2 智能学习报告生成

当前的学习报告（`generate_report()`）只包含统计信息。

- **任务**：扩展这个功能，设计一个更智能的学习报告生成器。
  - 要求：能够分析用户的**学习轨迹**、识别**知识盲点**、**推荐下一步学习内容**。
  - 问题：这需要用到哪些记忆类型和检索策略？请详细说明。

### 4.3 多用户数据隔离方案

假设你要将这个学习助手部署为多用户的 Web 服务，每个用户都有独立的记忆和知识库。

- **任务**：设计数据隔离方案。
  - 如何在 **Qdrant** 中实现用户级别的数据隔离？（Collection 级别 vs Payload 过滤）
  - 如何在 **Neo4j** 中实现用户级别的数据隔离？（独立图空间 vs 节点属性标记）
  - 如何优化多用户场景下的检索性能？

---

## 5. 语义记忆与知识图谱 ⭐⭐⭐ 理论/实践

语义记忆使用了 Neo4j 图数据库来存储知识图谱。

### 5.1 知识图谱质量评估

在 8.2.5 节的语义记忆实现中，系统会自动提取实体和关系构建知识图谱。

- **分析**：这种自动提取的准确性如何？在什么情况下可能会提取出错误的实体或关系？
- **任务**：设计一个「**知识图谱质量评估**」机制。
  - 指标建议：实体一致性、关系准确性、图连通性、冗余度等。
  - 请说明如何自动化检测低质量的三元组。

### 5.2 图查询能力设计

知识图谱的一个重要优势是支持复杂的关系推理。

- **任务**：设计一个查询场景，充分利用 Neo4j 的图查询能力。
  - 要求：使用多跳关系、路径查找、社区发现等高级特性。
  - 请提供 Cypher 查询语句，并说明这是纯向量检索无法完成的任务。

### 5.3 混合检索 vs 纯向量检索

对比语义记忆的「**向量检索 + 图检索**」混合策略与纯向量检索：

- 在什么类型的查询中，图检索能够带来显著的性能提升？
- 请用**具体例子**说明。
- 分析：混合检索的额外开销（图查询延迟、存储成本）是否值得？

---

## 学习检查清单

完成本章习题后，请确认你是否能够：

- [ ] 解释四种记忆类型的核心区别与评分机制差异
- [ ] 针对具体应用场景设计记忆系统的组合方案
- [ ] 实现至少一种改进的分块或检索策略
- [ ] 设计包含多因素加权评分的遗忘/归档机制
- [ ] 为 RAG + 记忆系统构建多用户架构方案
- [ ] 编写利用图数据库优势的 Cypher 查询

---

## 参考文献

[1] Atkinson, R. C., & Shiffrin, R. M. (1968). Human memory: A proposed system and its control processes. In *Psychology of learning and motivation* (Vol. 2, pp. 89-195). Academic Press.

[2] Lewis, P., et al. (2020). Retrieval-augmented generation for knowledge-intensive NLP tasks. *Advances in Neural Information Processing Systems*, 33, 9459–9474.

[3] Wang, L., et al. (2024). A survey on large language model based autonomous agents. *Frontiers of Computer Science*, 18(6), 186345.
